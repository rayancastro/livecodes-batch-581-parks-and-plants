<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= @garden.banner_url %>');">
  <h1><%= @garden.name %></h1>
</div>
<div class="container mt-3 text-center">
  <div class="row">
    <% @garden.plants.each do |plant| %>
      <div class="col-4">
        <div class="plant-card" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= plant.image_url %>');">
          <h4><%= link_to plant.name, plant_path(plant)  %></h4>

          <%= link_to "X", plant_path(plant), class: "card-action plant-destroy", method: :delete, data: { confirm: "Are you sure?"} %>
          <%= link_to "+", new_plant_plant_tag_path(plant), class: "add-tag card-action" %>

          <div class="card-tags">
            <% plant.tags.each do |tag| %>
              <span><%= tag.name %></span>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to "Add Plant", new_garden_plant_path(@garden) %>
</div>
